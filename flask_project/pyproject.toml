[tool.poetry]
name = "flask-project"
version = "0.1.0"
description = ""
authors = ["Vishal <vishal.kwd@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.13"
pytest = "^8.3.3"

[tool.poetry.scripts]
run_flask_project = "flask_project.test:start"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
